### Backend devlopment
1. Cretae git repo
git init

2. add .gitignore file

3. create .env file for environment variables  (for the security of DB since the repo is public -- stores the sensitive info --- provide a port no which is not assigned to any ) and read data from the .env 

4. Generate package.json
npm init -y

5. create express application

6. connect to database

7. Add middlewares(body parsers,err handling middlewares)

8. Design Schemas and create models

9.Design REST api's for all resources



### cookies are atteched to the req 